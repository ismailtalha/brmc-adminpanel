import { PositivenumberDirective } from './positivenumber.directive';

describe('PositivenumberDirective', () => {
  it('should create an instance', () => {
    const directive = new PositivenumberDirective();
    expect(directive).toBeTruthy();
  });
});
